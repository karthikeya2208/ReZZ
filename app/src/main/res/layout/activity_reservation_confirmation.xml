<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/confirmationLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="#F5F5F5">

    <!-- Success Animation -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottieSuccess"
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:lottie_autoPlay="true"
        app:lottie_loop="false"
        app:lottie_rawRes="@raw/success_tick" />

    <!-- Emoji-enhanced Confirmation Text -->
    <TextView
        android:id="@+id/textConfirmationMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🎉 Thank you for your reservation!\n\n🧾 Reservation No: XXXX"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:gravity="center"
        android:textAlignment="center"
        android:paddingTop="16dp"
        android:lineSpacingExtra="6dp" />

    <!-- Subtext -->
    <TextView
        android:id="@+id/textConfirmationSubtext"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="We look forward to hosting you! 😊"
        android:textSize="16sp"
        android:textColor="#666666"
        android:gravity="center"
        android:textAlignment="center"
        android:paddingTop="12dp" />

</LinearLayout>
